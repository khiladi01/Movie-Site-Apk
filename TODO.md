# TODO: Fix CORS Error for Local Development

## Backend Fixes
- [ ] Fix CORS configuration in backend/server.js: Define app first, set proper CORS options for localhost:5173

## Frontend Fixes
- [ ] Update fetch URLs in frontend/src/pages/login.jsx to use localhost:5000
- [ ] Update fetch URLs in frontend/src/pages/registration.jsx to use localhost:5000
- [ ] Update fetch URLs in frontend/src/pages/user.jsx to use localhost:5000
- [ ] Update fetch URLs in frontend/src/pages/admin.jsx to use localhost:5000
- [ ] Update fetch URLs in frontend/src/pages/profile.jsx to use localhost:5000
- [ ] Update fetch URLs in frontend/src/components/navbar.jsx to use localhost:5000

## Testing
- [ ] Start backend server
- [ ] Start frontend server
- [ ] Test login/registration to ensure no CORS errors
